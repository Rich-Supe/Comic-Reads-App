extends layout

include shelf.pug 

block content
    if(userShelves)
        div(class="user__shelves")
        h1 Your Shelves 
            each shelf in userShelves
                h2 Shelf: #{shelf.name}
                    +shelf(shelf.id)

//- div(class)