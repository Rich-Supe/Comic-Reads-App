extends layout.pug
include utils.pug 
block content
    link(rel="stylesheet" href="/stylesheets/results.css")
    div(class='frame')
        each result in results 
            +comicCard( result.coverUrl, result.title, result.author, result.genre, result.id)